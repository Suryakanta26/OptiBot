fastapi==0.141.1
uvicorn[standard]==0.52.0
pydantic==2.10.4
litellm==1.94.1
python-dotenv==1.2.2
numpy==2.5.1

# Called directly for GET /v1/models on the LiteLLM gateway and for the TLS
# bootstrap (litellm.client_session), so it is no longer a transitive-only dep.
# Pinned inside litellm 1.57.0's declared window (httpx>=0.23.0,<0.28.0).
httpx==0.28.1

# Note: litellm pulls in `openai` unpinned. Every call now goes through
# litellm's openai-compatible route, which constructs openai.OpenAI(...), so
# that version matters. Verified working with openai 2.50.0. If a fresh install
# starts raising unexpected-keyword errors from the OpenAI SDK, pin openai to
# the 1.59.x line litellm 1.57.0 was released against.

# Optional but recommended: real semantic embeddings for RAG + semantic cache.
# These are computed locally and never go through the LiteLLM gateway. The app
# falls back to a lightweight lexical embedder if this is not installed, so
# `pip install -r requirements.txt` works even without it.
sentence-transformers==5.6.1

# Standard LLM workflow, document ingestion, and graph orchestration.
langchain==1.3.11
langchain-community==0.4.1
langchain-text-splitters==1.1.2
langgraph==1.2.6
langchain-litellm==0.7.0

# Optional semantic-cache engine. OptiBot loads it lazily and retains the local
# semantic implementation when the package/backend is unavailable.
gptcache==0.1.44
faiss-cpu==1.13.2
